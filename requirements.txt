# Note: json, smtplib, email and time are part of the Python standard library
# and must NOT be installed via pip.
python-dotenv==1.0.1
requests==2.32.3
python-dateutil==2.9.0.post0
psutil==6.0.0
colorama==0.4.6
flask==3.1.3
playwright==1.60.0
